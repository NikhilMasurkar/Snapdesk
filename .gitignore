# This repo's root now only holds the UserMenuList app (see UserMenuList/.gitignore
# for its own ignores) plus sibling apps that are separate, independent repos.

.DS_Store

# sibling apps — each is its own independent git repo, not part of this one
/BusinessDashboard/
/Admin/
/Promotional_Website/
